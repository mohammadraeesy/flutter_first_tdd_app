import 'package:flutter/material.dart';
class Login {
  String username;
  String password;
 // int status;
  // if status = 0 it s mean inputed
  // if status = 1 it s mean loading
  // if status = 2 it s mean login sucsessfuly
  // if status = 3 it s mean there is something wrong
  Login({@required this.username,@required this.password,});
 // Login.whithStatuse({@required this.Password, @required this.userName , @required this.status});
}